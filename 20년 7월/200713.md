## 테스트
```Java
// main
import java.io.BufferedReader;
import java.io.InputStreamReader;

// 계산기 만들기
// 계산기에 들어갈 처리
// 1.더하기 -> 2개의 변수를 받아서 더하기 처리를함, 빼기, 곱하기, 나누기도 동일함
// 숫자카운트 -> 받은 변수에서 지정한 숫자가 몇번 들어가 있는지 숫자를 센다

// 숫자 문자 숫자 입력받음 -> 각 문자별로 메소드 구현
// 숫자 입력시 정수 실수 판별(메소드 안에서 구현)
// +,-,/,*,&로 입력 받음

public class test {
static int cal1,cal2,cal3,count1;
static double cnt1,cnt2,cnt3,count2;
	public static void main(String[] args) {
		CalculateClass C1 = new CalculateClass();
		try {
			InputStreamReader ir = new InputStreamReader(System.in);
			BufferedReader br = new BufferedReader(ir);
			String num1 = br.readLine();
			String cal = br.readLine();
			String num2 = br.readLine();
			if(isInteger(num1)==true) {
				cal1 = Integer.parseInt(num1);
			}
			if(isDouble(num1)==true) {
				cnt1 = Double.parseDouble(num1);
			}
			if(isInteger(num2)==true) {
				cal2 = Integer.parseInt(num2);
			}
			if(isDouble(num2)==true) {
				cnt2 = Double.parseDouble(num2);
			}
			if(isInteger(num1)==true&&isInteger(num2)==true) {
				switch(cal) {
				case "+":
					System.out.println(C1.add(cal1, cal2));
					count1=C1.add(cal1,cal2);
					break;
				case "-":
					System.out.println(C1.sub(cal1,cal2));
					count1=C1.sub(cal1,cal2);
					break;
				case "*":
					System.out.println(C1.mul(cal1, cal2));
					count1=C1.mul(cal1,cal2);
					break;
				case "/":
					System.out.println(C1.div(cal1, cal2));
					count1=C1.div(cal1, cal2);
					break;
				case "&":
					System.out.println(C1.count(cal1, cal2));
					break;
				}
			}
			else if(isInteger(num1)==true&&isDouble(num2)==true) {
				switch(cal) {
				case "+":
					System.out.println(C1.Dadd(cal1, cnt2));
					count2=C1.Dadd(cal1, cnt2);
					break;
				case "-":
					System.out.println(C1.Dsub(cal1, cnt2));
					count2=C1.Dsub(cal1, cnt2);
					break;
				case "*":
					System.out.println(C1.Dmul(cal1, cnt2));
					count2=C1.Dmul(cal1, cnt2);
					break;
				case "/":
					System.out.println(C1.Ddiv(cal1, cnt2));
					count2=C1.Ddiv(cal1, cnt2);
					break;
				}
			}
			else if(isDouble(num1)==true&&isInteger(num2)==true) {
				switch(cal) {
				case "+":
					System.out.println(C1.Dadd(cnt1, cal2));
					count2=C1.Dadd(cnt1, cal2);
					break;
				case "-":
					System.out.println(C1.Dsub(cnt1, cal2));
					count2=C1.Dsub(cnt1, cal2);
					break;
				case "*":
					System.out.println(C1.Dmul(cnt1, cal2));
					count2=C1.Dmul(cnt1, cal2);
					break;
				case "/":
					System.out.println(C1.Ddiv(cnt1, cal2));
					count2=C1.Ddiv(cnt1, cal2);
					break;
				case "&":
					System.out.println(C1.Dcount(cnt1, cal2));
					break;
				}
			}
			else if(isDouble(num1)==true&&isDouble(num2)==true){
				switch(cal) {
				case "+":
					System.out.println(C1.Dadd(cnt1, cnt2));
					count2=C1.Dadd(cnt1, cnt2);
					break;
				case "-":
					System.out.println(C1.Dsub(cnt1, cnt2));
					count2=C1.Dsub(cnt1, cnt2);
					break;
				case "*":
					System.out.println(C1.Dmul(cnt1, cnt2));
					count2=C1.Dmul(cnt1, cnt2);
					break;
				case "/":
					System.out.println(C1.Ddiv(cnt1, cnt2));
					count2=C1.Ddiv(cnt1, cnt2);
					break;
				}
			}
			for(;;) {
				String calc = br.readLine();
				String num3 = br.readLine();
				if(isInteger(num3)==true) {
					cal3 = Integer.parseInt(num3);
				}
				if(isDouble(num3)==true) {
					cnt3 = Double.parseDouble(num3);
				}
				
				if(isInteger(num3)==true) {
					switch(cal) {
					case "+":
						System.out.println(C1.add(count1, cal3));
						count1=C1.add(count1, cal3);
						break;
					case "-":
						System.out.println(C1.sub(count1,cal3));
						count1=C1.sub(count1, cal3);
						break;
					case "*":
						System.out.println(C1.mul(count1, cal3));
						count1=C1.mul(count1, cal3);
						break;
					case "/":
						System.out.println(C1.div(count1, cal3));
						count1=C1.div(count1, cal3);
						break;
					case "&":
						System.out.println(C1.count(count1, cal3));
						break;
					}
				}
				else if(isDouble(num3)==true) {
					switch(cal) {
					case "+":
						System.out.println(C1.Dadd(count2, cnt3));
						count2=C1.Dadd(count2, cnt3);
						break;
					case "-":
						System.out.println(C1.Dsub(count2, cnt3));
						count2=C1.Dsub(count2, cnt3);
						break;
					case "*":
						System.out.println(C1.Dmul(count2, cnt3));
						count2=C1.Dmul(count2, cnt3);
						break;
					case "/":
						System.out.println(C1.Ddiv(count2, cnt3));
						count2=C1.Ddiv(count2, cnt3);
						break;
					}
				}
				
			}
		}catch(Exception e) {}
	}

	public static boolean isDouble(String num) {
		try {
			Double.parseDouble(num);
			return true;
		}catch(NumberFormatException e) {
			return false;
		}
	}

	public static boolean isInteger(String num) {
		try {
			Integer.parseInt(num);
			return true;
		}catch(NumberFormatException e) {
			return false;
		}
	}
	
	

}
```
```Java
// 계산 클래스
public class CalculateClass {
	
	public int add(int a, int b) {
		int result = a + b;
		return result;
	}
	
	public double Dadd(double a, double b) {
		double result = a+b;
		return result;
	}
	
	public int sub(int a, int b) {
		int result = a-b;
		return result;
	}
	public double Dsub(double a, double b) {
		double result = a-b;
		return result;
	}
	public int mul(int a, int b) {
		int result = a*b;
		return result;
	}
	public double Dmul(double a,double b) {
		double result = a*b;
		return result;
	}
	public int div(int a, int b) {
		int result = a/b;
		return result;
	}
	public double Ddiv(double a, double b) {
		double result = a/b;
		return result;
	}
	
	public int count(int a, int b) {
		int cnt=0;
		while(a!=0) {
			if(a%10 == b) {
				cnt++;
				a/=10;
			}
			else {
				cnt/=10;
			}
		}
		return cnt;
	}
	public double Dcount(double a, int b) {
		int cnt = 0;
		while(a!=0) {
			if(a%10==b) {
				cnt++;
				a/=10;
			}
			else {
				cnt/=10;
			}
		}
		return cnt;
	}
}
```
- 계산에 사용하는 클래스는 저번에 푼 걸 그대로 메소드로 구현함
- 메인 클래스가 문제인데 실수와 정수 판별 메소드를 구현해 정수,실수 조건에 맞춰서 각각 조건문으로 나누어서 계산했음

### FeedBack
- 한 줄에 동시에 입력 실패, readLine을 사용해서 한 줄 한 줄씩 입력 받음
- 조건만을 직관적으로 생각해서 모두 조건문을 걸어서 사용함, 이번 역시 직관적으로 풀었고 조건문을 걸어서 빠진 에러코드 부분이 있을 확률이 높음
- 중복이 너무 많고 간결하지 못함으로 구현에 있어서 에로사항이 있을 것 같음
- 일단 이렇게 짠 게 구글링, 내가 생각한 것, 강의 내용 참조해서 푼 것임으로 만일 부족한 부분이 있다면 미쳐 생각치 못 한 부분이거나 부족한 부분임 