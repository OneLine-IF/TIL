### 200909_1일1공.md 파일 생성

# C 언어 스택 공부 + 파이썬 클래스, 상속 공부

```C
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#include<string.h>
#include<stdlib.h>

struct Node {
	char e;
	int rank;
	struct Node* next;
};
typedef struct Node node;

node* getNode(node** p) {
	(*p) = (node*)malloc(sizeof(node));
	(*p)->next = NULL;
	return (*p);
}

void push(node* stack, char e, int prior) {
	struct Node* p = getNode(&p);
	p->e = e; p->rank = prior;

	p->next = stack->next;
	stack->next = p;
}

char pop(struct Node* stack) {
	struct Node* p = stack->next;
	char tmp = p->e;
	stack->next = p->next;
	free(p);
	return tmp;
}

int priority(char op) {
	if (op == '*' || op == '/') return 2;
	else if (op == '+' || op == '-') return 1;
	else return 0;
}

void convert(char* infix, char* postfix) {
	node* stack = getNode(&stack);
	int cnt = 0;

	for (int i = 0; i < strlen(infix); i++) {
		int prior = priority(infix[i]);
		if (infix[i] >= '0' && infix[i] <= '9') {
			postfix[cnt] = infix[i];
			cnt++;
		}
		else if (infix[i] == '(') {
			push(stack, infix[i], prior);
		}
		else if (infix[i] == ')') {
			while (stack->next != NULL && prior < stack->next->rank) {
				postfix[cnt] = pop(stack);
				cnt++;
			}
			pop(stack);// delete ')' in the stack!
		}
		else {
			while (stack->next != NULL && prior <= stack->next->rank) {
				postfix[cnt] = pop(stack);
				cnt++;
			}
			push(stack, infix[i], prior);// add '+' or '-' in the stack!
		}
	}
	while (stack->next != NULL) {
		postfix[cnt] = pop(stack);
		cnt++;
	}
	postfix[cnt] = '\0';
}

int evaluate(char* postfix) {
	node* stack = getNode(&stack);

	for (int i = 0; i < strlen(postfix); i++) {
		if (postfix[i] >= '0' && postfix[i] <= '9') {
			push(stack, postfix[i] - '0', 0);
		}
		else if (postfix[i] == '+') {
			int b = pop(stack), a = pop(stack);
			push(stack, a + b, 0);
		}
		else if (postfix[i] == '-') {
			int b = pop(stack), a = pop(stack);
			push(stack, a - b, 0);
		}
		else if (postfix[i] == '*') {
			int b = pop(stack), a = pop(stack);
			push(stack, a * b, 0);
		}
		else if (postfix[i] == '/') {
			int b = pop(stack), a = pop(stack);
			push(stack, a / b, 0);
		}
	}
	return pop(stack);
}

void main() {
	int N;
	char infix[101], postfix[101];

	printf("How many times will you calculate? ");
	scanf("%d", &N);

	for (int i = 0; i < N; i++) {
		printf("\nInfix: ");
		scanf("%s", infix);
		convert(infix, postfix);
		printf("Postfix: %s\n", postfix);
		printf("Answer: %d\n", evaluate(postfix));
	}

}

```

```Python
# 과일이 각각 몇개 있는지 확인하는 프로그램
fruit = ["사과", "사과", "바나나", "딸기", "키위", "복숭아", "복숭아", "복숭아"] # 리스트

d = {} # 딕셔너리
# d = {"사과": 1}

for f in fruit:
  # f = "사과"

  if f in d: # "사과" 라는 key가 d 라는 딕셔너리에 들어있어?
    d[f] = d[f] + 1 # 그럼 "사과" 갯수를 하나 올려줘
  else:
    d[f] = 1 # 만약 "사과" 라는 애가 없으면, 그걸 딕셔너리에 넣고 벨류는 1로 만들어줘

print(d)



# 클래스: 함수 + 변수 모아놓은 거
# 클래스: 빵틀, 오브젝트: 빵

class Person:
  def __init__(self, name, age): # self를 첫 인자로 받고
    self.name = name
    self.age = age

  def say_hello(self, to_name):
    print("안녕! " + to_name + " 나는 " + self.name)

  def introduce(self):
    print("내 이름은 " + self.name + " 그리고 나는 " + str(self.age) + " 살이야")

wonie = Person("워니", 20)
wonie.introduce()

# 상속
class Police(Person):
  def arrest(self, to_arrest):
    print("넌 체포됐다, " + to_arrest)

class Programmer(Person):
  def program(self, to_program):
    print("다음엔 뭘 만들지? 아 이걸 만들어야겠다: " + to_program)

wonie = Person("워니", 20)
jenny = Police("제니", 21)
michael = Programmer("마이클", 22)

jenny.introduce()
jenny.arrest("워니")
michael.introduce()
michael.program("이메일 클라이언트")

# 1:13 부터~
```