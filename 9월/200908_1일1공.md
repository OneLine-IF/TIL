## 200908_1일1공

1. 문법 강의 2/3 수강하였고 내일 마무리 예정 + 자바 복습은 못함

```Python
z = "안녕"
print(z)

x = 2
y = 4
print(x ** y)# 제곱

z = """
안녕하세요
우혁입니다
"""
print(z)

print("안녕" + " 잘 지내니?")# 숫자랑 문자는 합치지 못함
print("너 혹시 몇살이니? " + str(4))# 이런 것을 '캐스팅'이라 함

if not 1 > 0 and 2 > 1:# 'else if'는 'elif'
  print("Hello")
else:
  print("Hi")

# 함수
def chat(name1, name2, age):
  print("%s: 안녕? 넌 몇 살이니?" % name1)
  print("%s: 나? 나는 %d" % (name2, age))

chat("알렉스", "윤하", 10)
chat("철수", "영희", 30)

def dsum(a, b):
  result = a + b;
  return result

d = dsum(1, 2)
print(d)

# for, while 문 // continue, break 도 적절히 사용
i = 0
while i < 3:# 무한 반복문 사용시 유용, 'while True:''
  print(i)
  chat("철수", "영희", 30)
  i = i + 1

for i in range(3):# 'i'는 0~2
  print(i)
  chat("철수", "영희", 30)

# 자료구조: 리스트, 튜플, 딕셔너리

# 리스트
x = [4,2,3,1]# x = list() <- 이렇게 선언하기도 함
y = ["hello", "world"]
z = ["hello", 1, 2, 3]

print(x + y)
print(x[0])# x의 0번째 자리에 있는 원소를 보여줌

x[3] = 10
print(x[3])

# '리스트'에서 자주 사용하는 함수
# 원소의 개수
num_elements = len(x)
print(num_elements)
# 정렬
y = sorted(x)
print(y)
# 원소의 합
y = sum(x)
print(y)
# 반복문을 이용하여 리스트의 원소 훑기
for n in x: 
  print(n)
# 인덱스값 찾기
print(x.index(3))
# 원소가 해당 리스트에 있는지 확인
if 3 in x:
  print("'3'이 있어요.")

# 튜플
x = (1,2,3)# x = tuple()
y = ('a','b','c')
z = (1,"hello","there")

print(x + y)
print('a' in y)
print(z.index(1))
# 튜플에서는 리스트와 다르게 assignment(할당) 불가 <- 가장 큰 차이점
# x[0] = 10 불가

# 딕셔너리 -> 'key'와 'value'로 나눠져 있는 것
x = {
  0: "우혁",
  1: "hello",
  "age": 20, 
}#x = dict()

print(x[0])
print(x[1])
print(x["age"])
# 리스트와 마찬가지로 딕셔너리에 어떤 키가 있는지 확인 가능
print("age" in x)
# .keys() -> 딕셔너리에 있는 모든 키들을 보여줌
print(x.keys())
# .values() -> 딕셔너리에 있는 모든 키들을 보여줌
print(x.values())
# 반복문을 이용해서 딕셔너리 훑기
for key in x:
  print("key: " + str(key))
  print("value: " + str(x[key]))
# 할당 + 추가 가능
x[0] = "워니"
print(x)
x["school"] = "한빛"
print(x)

## 53분 부터 프로그램 하나 짜보기
```



